# Quantora Orbit uses mock telemetry by default.
DATA_SOURCE=mock
INITIAL_BALANCE=1350

# Optional authenticated normalized WebSocket bridge.
# DATA_SOURCE=bridge
# MT5_BRIDGE_WS_URL=wss://your-private-bridge.example/ws
# MT5_BRIDGE_TOKEN=replace-with-a-long-random-token

# Optional existing FastAPI/MetaTrader5 adapter in this repository.
# Used when DATA_SOURCE=bridge and MT5_BRIDGE_WS_URL is not set.
MT5_BRIDGE_HTTP_URL=http://127.0.0.1:8000
MT5_BRIDGE_POLL_INTERVAL=1000
# Set the same long random value on the laptop bridge and the cloud server.
# BRIDGE_TOKEN=replace-with-a-long-random-token

# MT5 credentials belong only in a secret manager or local environment settings.
MT5_LOGIN=
MT5_PASSWORD=
MT5_SERVER=ICMarketsEU-MT5-5
# The launcher reads BRIDGE_HOST/BRIDGE_PORT for the local FastAPI listener.
# BRIDGE_HOST=127.0.0.1
# BRIDGE_PORT=8000
MT5_SYMBOLS=USTEC,XAUUSD

# Optional browser WebSocket origin restriction for production.
# WS_ALLOWED_ORIGIN=https://your-domain.example
# NEXT_PUBLIC_WS_URL=wss://your-dashboard.example/ws

# Optional manual agent attribution for the existing Python adapter.
# AGENT_MAP=[{"id":"NQ-ALPHA","symbols":["USTEC"],"magics":[111001],"tags":[]},{"id":"NQ-SIGMA","symbols":["USTEC"],"magics":[111002],"tags":[]},{"id":"XAU-PRIME","symbols":["XAUUSD"],"magics":[222001],"tags":[]},{"id":"XAU-FLASH","symbols":["XAUUSD"],"magics":[222002],"tags":[]}]
